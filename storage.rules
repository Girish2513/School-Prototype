rules_version = '2';

service firebase.storage {
  match /b/{bucket}/o {
    // Allow authenticated users to write to the popup-banners folder
    match /popup-banners/{allPaths=**} {
      allow write: if request.auth != null;
    }
  }
}